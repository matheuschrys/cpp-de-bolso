import type { ChapterId, Difficulty } from "../types/study";
import type { QuestionSeed } from "./questionBank";

const difficulties: Difficulty[] = ["Média", "Difícil", "Muito difícil"];
const seeds: QuestionSeed[] = [];

const add = (chapter: ChapterId, theme: string, kind: NonNullable<QuestionSeed["kind"]>, prompt: string, answer: string, distractors: string[], explanation: string, code?: string, tags: string[] = []) => {
  seeds.push({ chapter, theme, kind, difficulty: difficulties[seeds.length % difficulties.length], prompt, answer, distractors, explanation, code, tags: [theme, ...tags] });
};

// 25 novas questões de arquivos: abertura, cursor, flags, binário e persistência.
for (let i = 0; i < 5; i += 1) {
  const a = String.fromCharCode(65 + i * 2);
  const b = String.fromCharCode(66 + i * 2);
  add("17", "Modos de abertura", "saída do programa", "Considerando arquivo inicialmente vazio, qual conteúdo fica após as duas escritas?", `${a}${b}`, [`Somente ${a}`, `Somente ${b}`, `${b}${a}`, "O arquivo fica vazio"], "`ios::app` posiciona toda escrita no final, preservando o conteúdo que já existia.", `std::ofstream a("log.txt", std::ios::app);\na << '${a}'; a.close();\nstd::ofstream b("log.txt", std::ios::app);\nb << '${b}';`, ["ios::app", "ofstream"]);
  add("17", "Ponteiros de arquivo", "erro de compilação", "Qual alteração posiciona corretamente a escrita no registro de índice 2?", "Usar `seekp(2 * sizeof(Registro))` antes de `write`.", ["Usar `seekg` antes de `write`.", "Usar `tellp` antes de `read`.", "Abrir somente com `ios::app`.", "Trocar `write` por `getline`."], "`seekp` controla a posição de put/escrita; `seekg` é o cursor de get/leitura.", `Registro r{${i + 1}};\narq.seekg(2 * sizeof(Registro));\narq.write(reinterpret_cast<const char*>(&r), sizeof r);`, ["seekp", "write"]);
  add("17", "Flags do fluxo", "conceito", `Depois de ler ${i + 1} registro(s) e alcançar EOF, qual sequência permite voltar ao início e ler de novo?`, "Chamar `clear()` e depois `seekg(0)`.", ["Chamar apenas `seekg(0)`.", "Chamar `eof()` e depois `good()`.", "Fechar sem abrir novamente.", "Usar `seekp(0)` apenas."], "As flags de erro/fim bloqueiam novas operações. Limpe o estado antes de reposicionar o cursor de leitura.", `while (arq.get(c)) {}\n// qual é a sequência correta aqui?`, ["clear", "eof"]);
  add("17", "Texto e binário", "conceito", `Ao persistir o campo textual ${["nome", "descrição", "cidade", "matrícula", "observação"][i]} de tamanho variável em binário, qual estratégia é segura?`, "Gravar o tamanho e, em seguida, os caracteres da string.", ["Aplicar `write` no objeto `std::string` inteiro.", "Gravar somente o ponteiro de `c_str()`.", "Usar `sizeof(std::string)` como texto.", "Converter o endereço para `int`."], "A representação interna de `std::string` não é um formato de arquivo. Tamanho mais bytes permite reconstrução controlada.", undefined, ["serialização", "binário"]);
  add("17", "Leitura segura", "implementação", `Ao ler o arquivo de ${["alunos", "produtos", "pedidos", "eventos", "notas"][i]}, qual condição evita processar uma linha inexistente no fim?`, "Usar a própria leitura como condição do laço.", ["Usar `while (!arq.eof())`.", "Testar `arq.good()` somente depois do corpo.", "Ler uma linha extra antes do laço.", "Chamar `bad()` em toda linha."], "A tentativa de leitura informa se há conteúdo válido. `eof()` só se torna verdadeiro após uma leitura que falhou por fim.", `std::string linha;\nwhile (std::getline(arq, linha)) {\n  processar(linha);\n}`, ["getline", "eof"]);
}

// 25 de listas: estados, inserção, remoção e memória.
for (let i = 0; i < 5; i += 1) {
  const x = 10 + i; const y = 20 + i; const z = 30 + i;
  add("listas", "Lista simplesmente encadeada", "saída do programa", "Qual valor é impresso ao seguir o ponteiro `prox` uma vez?", String(y), [String(x), String(z), "nullptr", "Erro de compilação"], "`inicio->prox` aponta para o segundo nó, cujo valor é o segundo elemento inserido.", `struct No { int valor; No* prox; };\nNo* inicio = new No{${x}, nullptr};\ninicio->prox = new No{${y}, new No{${z}, nullptr}};\nstd::cout << inicio->prox->valor;`, ["No", "prox"]);
  add("listas", "Inserção", "estado final", "Após inserir no início, qual é a nova cabeça da lista?", String(z), [String(x), String(y), "nullptr", "O último nó"], "A inserção no início liga o novo nó à cabeça antiga e atualiza `inicio` para o novo nó.", `No* novo = new No{${z}, inicio};\ninicio = novo;`, ["inserção no início"]);
  add("listas", "Remoção", "implementação", `Na remoção inicial da lista de ${["tarefas", "clientes", "compras", "vértices", "registros"][i]}, qual ponteiro deve ser salvo para não vazar memória?`, "O ponteiro para o nó que será removido.", ["Apenas `inicio->prox->prox`.", "Um ponteiro para `nullptr`.", "O valor inteiro do nó.", "Nenhum; `delete inicio` basta."], "Guarde o nó antigo, avance a cabeça e só então aplique `delete` no nó removido.", `No* removido = inicio;\ninicio = inicio->prox;\n// falta liberar corretamente`, ["delete", "remoção"]);
  add("listas", "Complexidade", "complexidade", `Qual é a complexidade para acessar a posição ${i + 3} de uma lista simplesmente encadeada, começando apenas pela cabeça?`, "O(n), pois é necessário percorrer os nós até a posição.", ["O(1), como em vector.", "O(log n), por dividir a lista.", "O(n log n).", "O(2ⁿ)."], "Não existe acesso por índice direto em lista encadeada; cada avanço segue um ponteiro.", undefined, ["O(n)", "acesso"]);
  add("listas", "Casos especiais", "erro de compilação", `Em uma rotina de remoção número ${i + 1}, qual caso exige atenção extra ao apagar o último elemento?`, "Lista com um único nó: a cabeça precisa virar `nullptr` depois do delete.", ["Lista com muitos nós, pois delete não funciona.", "Lista vazia, pois sempre há um último nó.", "Lista circular, pois não usa ponteiros.", "Nenhum caso especial existe."], "Com um único nó, não há anterior para religar; a estrutura passa a ser vazia.", undefined, ["lista vazia", "um nó"]);
}

// 20 de pilhas e 20 de filas.
for (let i = 0; i < 5; i += 1) {
  const a = i + 3; const b = i + 7; const c = i + 9;
  add("pilhas", "std::stack", "saída do programa", "Qual é a saída após as operações na pilha?", String(b), [String(a), String(c), "0", "A pilha fica vazia"], "A última inserção é removida por `pop`; o novo topo é o valor inserido antes dela.", `std::stack<int> p;\np.push(${a}); p.push(${b}); p.push(${c});\np.pop();\nstd::cout << p.top();`, ["push", "pop", "top"]);
  add("pilhas", "LIFO", "estado final", `Qual é o estado da pilha, do topo para a base, após \`push(${a}), push(${b}), pop(), push(${c})\`?`, `${c}, ${a}`, [`${a}, ${c}`, `${b}, ${c}`, `${c}, ${b}, ${a}`, "vazia"], `A pilha remove o último inserido (${b}) antes de receber ${c}.`, undefined, ["LIFO", "estado final"]);
  add("pilhas", "Underflow", "conceito", `Em uma pilha usada no módulo ${i + 1}, qual operação é inválida sem checar \`empty()\` quando ela está vazia?`, "`top()` e `pop()`.", ["`size()` apenas.", "`empty()` apenas.", "`push()` apenas.", "Nenhuma; stack cria elemento automático."], "Pilha vazia não possui topo nem elemento para remover. A checagem deve anteceder a operação.", undefined, ["underflow", "empty"]);
  add("pilhas", "Aplicações", "conceito", `Qual cenário do caso ${i + 1} é naturalmente modelado por uma pilha?`, "Desfazer ações ou acompanhar chamadas recursivas.", ["Atendimento por ordem de chegada.", "Busca em vetor ordenado.", "Ordenação estável por intercalação.", "Persistência de arquivos."], "O evento mais recente é o primeiro que deve ser recuperado, exatamente a regra LIFO.", undefined, ["recursão", "desfazer"]);
  add("pilhas", "Complexidade", "complexidade", `Qual é a complexidade típica de \`push\`, \`pop\` e \`top\` em uma pilha adequada no cenário ${i + 1}?`, "O(1) para cada operação.", ["O(n) para cada operação.", "O(log n) para pop.", "O(n²) para push.", "Depende somente do valor guardado."], "As operações atuam apenas na extremidade topo, sem percorrer a estrutura.", undefined, ["O(1)", "top"]);

  add("filas", "std::queue", "saída do programa", "Qual valor é impresso pela fila após as operações?", String(b), [String(a), String(c), "0", "A fila fica vazia"], `A fila remove o primeiro que entrou (${a}); o novo front é ${b}.`, `std::queue<int> f;\nf.push(${a}); f.push(${b}); f.push(${c});\nf.pop();\nstd::cout << f.front();`, ["push", "pop", "front"]);
  add("filas", "FIFO", "estado final", `Qual é a ordem da frente para o fim após enqueue(${a}), enqueue(${b}), dequeue(), enqueue(${c})?`, `${b}, ${c}`, [`${c}, ${b}`, `${a}, ${c}`, `${c}, ${a}`, "vazia"], `A remoção retira ${a}, que foi o primeiro a entrar. O restante preserva a ordem FIFO.`, undefined, ["FIFO", "estado final"]);
  add("filas", "priority_queue", "conceito", `No cenário de escalonamento ${i + 1}, qual afirmação diferencia \`priority_queue\` de \`queue\`?`, "`priority_queue` entrega o elemento de maior prioridade, não necessariamente o primeiro inserido.", ["As duas são sempre FIFO.", "priority_queue não permite push.", "queue ordena automaticamente os elementos.", "deque e queue são sinônimos completos."], "A prioridade muda a regra de retirada; por padrão, o maior valor fica no topo.", undefined, ["prioridade", "FIFO"]);
  add("filas", "BFS", "conceito", `Na busca em largura do grafo ${i + 1}, qual estrutura sustenta naturalmente o percurso?`, "Fila, pois processa primeiro os vértices descobertos mais cedo.", ["Pilha, pois sempre vai mais fundo.", "Lista sem ponteiros.", "Arquivo binário.", "Heap sort."], "BFS explora por camadas e por isso precisa respeitar a ordem de descoberta FIFO.", undefined, ["BFS", "fila"]);
  add("filas", "Underflow", "erro de compilação", `Antes de chamar \`front()\` na fila de atendimento ${i + 1}, qual cuidado é necessário?`, "Verificar se a fila não está vazia com `empty()`.", ["Chamar `size()` depois de front.", "Usar `back()` primeiro.", "Fazer `push(0)` sempre.", "Nenhum: front retorna nullptr."], "Fila vazia não possui frente válida. A operação não deve ser feita sem a pré-condição.", undefined, ["empty", "underflow"]);
}

// 25 de Big O: padrões de laço e operações.
for (let i = 0; i < 5; i += 1) {
  add("big-o", "Laço simples", "complexidade", "Qual é a complexidade de tempo do trecho?", "O(n).", ["O(1).", "O(log n).", "O(n log n).", "O(n²)."], "O corpo é executado proporcionalmente a n; dobrar n aproximadamente dobra as iterações.", `for (int i = 0; i < n; ++i) soma += i + ${i};`, ["laço", "O(n)"]);
  add("big-o", "Laço aninhado", "complexidade", `No processamento da matriz ${i + 1}, qual é a complexidade de tempo dos dois laços aninhados?`, "O(n²).", ["O(n).", "O(log n).", "O(n log n).", "O(1)."], "Para cada uma das n iterações externas, o interno executa n vezes: n × n.", `for (int i = 0; i < n; ++i)\n  for (int j = 0; j < n; ++j) usar(i, j);`, ["aninhado", "O(n²)"]);
  add("big-o", "Laço logarítmico", "complexidade", `No limite de tamanho ${2 ** (i + 4)}, qual é a ordem assintótica do laço que dobra i em cada passo?`, "O(log n).", ["O(1).", "O(n).", "O(n²).", "O(2ⁿ)."], "Cada passo elimina metade do trabalho restante em escala; o número de duplicações até n é log₂n.", `for (int i = 1; i < n; i *= 2) usar(i);`, ["logaritmo", "O(log n)"]);
  add("big-o", "Laços independentes", "complexidade", `No relatório ${i + 1}, qual é a complexidade de dois laços O(n) executados um após o outro?`, "O(n), pois O(n) + O(n) simplifica para O(n).", ["O(n²), pois há dois laços.", "O(log n).", "O(1).", "O(2n²)."], "Laços sequenciais somam custos; constantes são descartadas em Big O.", `for (int i=0;i<n;++i) a();\nfor (int j=0;j<n;++j) b();`, ["soma", "O(n)"]);
  add("big-o", "Espaço", "complexidade", `Para copiar ${10 * (i + 1)} elementos em um vetor auxiliar, qual é o espaço assintótico da estrutura extra?`, "O(n).", ["O(1).", "O(log n).", "O(n²) sempre.", "Não existe espaço auxiliar."], "O vetor adicional cresce proporcionalmente à entrada, independentemente do tempo do algoritmo.", `std::vector<int> copia(n);`, ["espaço", "O(n)"]);
}

// 30 de ordenação: cinco ângulos de prova para cada uma das seis famílias.
const sorts = [
  { name: "Bubble Sort", behavior: "compara vizinhos e troca quando estão invertidos", complexity: "O(n²) no pior caso", snippet: "if (v[j] > v[j + 1]) std::swap(v[j], v[j + 1]);", property: "Pode ser estável e, com uma flag de troca, tem melhor caso O(n)." },
  { name: "Selection Sort", behavior: "procura o menor restante e troca com a posição atual", complexity: "O(n²) mesmo em entrada quase ordenada", snippet: "int menor = i; for (int j=i+1;j<n;++j) if(v[j]<v[menor]) menor=j;", property: "Em geral não é estável e continua fazendo O(n²) comparações mesmo quase ordenado." },
  { name: "Insertion Sort", behavior: "insere uma chave deslocando a parte já ordenada", complexity: "O(n²) no pior caso, mas bom em dados quase ordenados", snippet: "while (j >= 0 && v[j] > chave) { v[j+1] = v[j]; --j; }", property: "É estável na implementação usual e se adapta bem a dados quase ordenados." },
  { name: "Merge Sort", behavior: "divide o vetor e intercala partes ordenadas", complexity: "O(n log n) com memória auxiliar", snippet: "mergeSort(esq); mergeSort(dir); intercalar(v);", property: "É estável na implementação comum, mas precisa de memória auxiliar linear." },
  { name: "Quick Sort", behavior: "particiona elementos em torno de um pivô", complexity: "médio O(n log n), pior O(n²)", snippet: "int p = particionar(v, inicio, fim); quick(v, inicio, p-1);", property: "Geralmente é in-place, mas uma escolha ruim de pivô pode levar ao pior caso O(n²)." },
  { name: "Heap Sort", behavior: "usa heap para extrair repetidamente o maior elemento", complexity: "O(n log n) e não é estável", snippet: "construirHeap(v); for (int fim=n-1; fim>0; --fim) extrairMax(v, fim);", property: "Não é estável em geral e mantém O(n log n) também no pior caso." },
] as const;
for (const sort of sorts) {
  const otherSorts = sorts.filter((other) => other.name !== sort.name).slice(0, 4).map((other) => other.name);
  add("ordenacao", sort.name, "interpretação de código", "Qual algoritmo é identificado principalmente pelo padrão abaixo?", sort.name, otherSorts, `${sort.name} ${sort.behavior}; sua característica de custo é ${sort.complexity}.`, sort.snippet, ["identifique o algoritmo", "ordenação"]);
  add("ordenacao", sort.name, "interpretação de código", `Qual traço do código é mais característico de ${sort.name}?`, sort.behavior, sorts.filter((other) => other.name !== sort.name).slice(0, 4).map((other) => other.behavior), `O padrão de implementação revela a ideia central: ${sort.behavior}.`, sort.snippet, ["identifique o algoritmo", "implementação"]);
  add("ordenacao", sort.name, "complexidade", `Qual afirmação de custo é correta para ${sort.name}?`, sort.complexity, sorts.filter((other) => other.name !== sort.name).slice(0, 4).map((other) => other.complexity), `A análise esperada é: ${sort.complexity}.`, sort.snippet, ["complexidade", "ordenação"]);
  add("ordenacao", sort.name, "conceito", `Qual observação de prova sobre ${sort.name} está correta?`, sort.property, sorts.filter((other) => other.name !== sort.name).slice(0, 4).map((other) => other.property), sort.property, undefined, ["estabilidade", "ordenação"]);
  add("ordenacao", sort.name, "implementação", `Qual ação descreve corretamente a etapa principal de ${sort.name} em uma execução?`, sort.behavior, sorts.filter((other) => other.name !== sort.name).slice(1, 5).map((other) => other.behavior), `Na execução de ${sort.name}, a etapa central é: ${sort.behavior}.`, `${sort.snippet}\n// vetor de teste: {${sort.name.length}, 4, 1}`, ["etapa", "ordenação"]);
}

// 10 busca, 10 ponteiros e 10 integradas.
for (let i = 0; i < 5; i += 1) {
  add("busca", "Busca linear", "complexidade", "Qual é a complexidade da busca que percorre até encontrar ou chegar ao fim?", "O(n) no pior caso.", ["O(1).", "O(log n).", "O(n log n).", "O(n²)."], "No pior caso o alvo está no final ou ausente, exigindo visitar todos os elementos.", `for (int x : v) if (x == ${i + 5}) return true;`, ["linear", "O(n)"]);
  add("busca", "Busca binária", "conceito", `Qual pré-condição é indispensável para usar \`std::binary_search\` no vetor ordenado do exercício ${i + 1}?`, "A coleção deve estar ordenada conforme a mesma comparação usada na busca.", ["A coleção deve ser uma lista encadeada.", "O alvo deve ser o primeiro item.", "O vector deve ter tamanho potência de dois.", "Nenhuma; a função ordena automaticamente."], "Busca binária decide qual metade descartar com base na ordem. Sem ordenação, essa decisão perde sentido.", `std::binary_search(v.begin(), v.end(), alvo);`, ["ordenado", "binary_search"]);
  add("ponteiros", "new e delete", "estado final", `Qual é a ordem segura ao remover o primeiro nó da lista ${i + 1}?`, "Guardar o nó antigo, avançar a cabeça, aplicar delete e evitar reutilizar o ponteiro removido.", ["Aplicar delete e depois acessar `inicio->prox`.", "Definir início como nullptr sempre.", "Copiar só o valor e nunca liberar memória.", "Usar `free` em um objeto criado com new."], "Salvar a referência permite religar a estrutura e liberar exatamente o nó certo, sem dangling pointer.", `No* removido = inicio;\ninicio = inicio->prox;\ndelete removido;`, ["delete", "dangling"]);
  add("ponteiros", "Acesso", "erro de compilação", `No nó ${i + 1}, qual operador acessa o campo \`valor\` quando \`no\` é um \`No*\` válido?`, "`no->valor`.", ["`no.valor`.", "`no::valor`.", "`*no.valor` sempre.", "`no[valor]`."], "`->` é a forma abreviada de desreferenciar o ponteiro e acessar um membro do objeto apontado.", undefined, ["->", "Node"]);
  add("listas", "Operações integradas", "estado final", `Uma lista começa vazia. Insira ${i + 1} no início, ${i + 2} no fim, remova o primeiro e insira ${i + 3} no início. Qual é a ordem final?`, `${i + 3}, ${i + 2}`, [`${i + 1}, ${i + 2}`, `${i + 2}, ${i + 3}`, `${i + 3}, ${i + 1}`, "vazia"], "A primeira inserção é removida; o novo valor entra como cabeça e o segundo permanece no fim.", undefined, ["integrada", "lista"]);
  add("pilhas", "Estruturas integradas", "estado final", `No analisador de expressão ${i + 1}, qual estrutura é mais adequada para validar parênteses aninhados?`, "Pilha, empilhando aberturas e desempilhando ao encontrar fechamentos compatíveis.", ["Fila FIFO.", "Busca binária.", "Arquivo de acesso aleatório.", "Selection sort."], "A última abertura ainda não fechada deve ser a primeira a ser verificada, que é exatamente LIFO.", undefined, ["integrada", "parênteses"]);
}

export const edbQuestionSeeds = seeds;
